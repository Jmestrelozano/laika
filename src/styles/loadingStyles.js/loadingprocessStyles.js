import { StyleSheet } from 'react-native';

export const loadingprocessStyles = StyleSheet.create({
  container: {
    display: 'flex',
    justifyContent: 'center',
  },
  text: {
    fontSize: 16,
    marginTop: 4,
    color: 'white',
    fontWeight: 'bold',
  },
});
